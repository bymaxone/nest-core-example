/**
 * @fileoverview Health route placeholder.
 *
 * Replaced with the Health Console status tiles and check list in a
 * follow-up task; this placeholder only proves the shell renders every route.
 *
 * @layer screen
 */

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'

export default function HealthPage() {
  return (
    <Card>
      <CardHeader accent>
        <CardTitle>Health</CardTitle>
        <CardDescription>The Health Console lands here.</CardDescription>
      </CardHeader>
      <CardContent>Content coming up.</CardContent>
    </Card>
  )
}
