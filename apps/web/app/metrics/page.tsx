/**
 * @fileoverview Metrics route placeholder.
 *
 * Replaced with the raw scrape and highlights panels in a follow-up task;
 * this placeholder only proves the shell renders every route.
 *
 * @layer screen
 */

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'

export default function MetricsPage() {
  return (
    <Card>
      <CardHeader accent>
        <CardTitle>Metrics</CardTitle>
        <CardDescription>The Metrics View lands here.</CardDescription>
      </CardHeader>
      <CardContent>Content coming up.</CardContent>
    </Card>
  )
}
