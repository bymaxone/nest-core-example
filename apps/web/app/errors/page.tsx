/**
 * @fileoverview Errors route placeholder.
 *
 * Replaced with the Error Envelope Playground trigger grid and viewer in a
 * follow-up task; this placeholder only proves the shell renders every route.
 *
 * @layer screen
 */

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'

export default function ErrorsPage() {
  return (
    <Card>
      <CardHeader accent>
        <CardTitle>Errors</CardTitle>
        <CardDescription>The Error Envelope Playground lands here.</CardDescription>
      </CardHeader>
      <CardContent>Content coming up.</CardContent>
    </Card>
  )
}
