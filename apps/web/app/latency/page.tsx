/**
 * @fileoverview Latency route placeholder.
 *
 * Replaced with the Latency Lab delay control and sample feed in a follow-up
 * task; this placeholder only proves the shell renders every route.
 *
 * @layer screen
 */

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'

export default function LatencyPage() {
  return (
    <Card>
      <CardHeader accent>
        <CardTitle>Latency</CardTitle>
        <CardDescription>The Latency Lab lands here.</CardDescription>
      </CardHeader>
      <CardContent>Content coming up.</CardContent>
    </Card>
  )
}
