/**
 * @fileoverview Pagination route placeholder.
 *
 * Replaced with the offset/cursor tabs and the corrupt-cursor demo in a
 * follow-up task; this placeholder only proves the shell renders every route.
 *
 * @layer screen
 */

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'

export default function PaginationPage() {
  return (
    <Card>
      <CardHeader accent>
        <CardTitle>Pagination</CardTitle>
        <CardDescription>The offset and cursor tabs land here.</CardDescription>
      </CardHeader>
      <CardContent>Content coming up.</CardContent>
    </Card>
  )
}
